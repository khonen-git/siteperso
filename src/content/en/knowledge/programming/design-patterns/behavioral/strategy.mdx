---
title: Strategy
description: Define a family of interchangeable algorithms.
---

import { MdxCard } from '@/components/mdx/MdxCard';
import { CodeBlock } from '@/components/features/knowledge/math/CodeBlock';
import { ConsoleOutput } from '@/components/mdx/ConsoleOutput';

# Strategy

<i>
    Nota bene : This content was generated by AI. I am working on this page to provide personal content of better quality.
</i>

<MdxCard>
Define a family of interchangeable algorithms.
</MdxCard>

## Intent

Pick an algorithm at runtime (sorting, pricing, export).

## Python example

<CodeBlock language="python">
{`class CardPayment:
    def pay(self, amount):
        return f"Paid {amount} by card"

class PaypalPayment:
    def pay(self, amount):
        return f"Paid {amount} by PayPal"

class Checkout:
    def __init__(self, strategy):
        self.strategy = strategy

    def process(self, amount):
        print(self.strategy.pay(amount))

Checkout(CardPayment()).process(49)
Checkout(PaypalPayment()).process(49)`}
</CodeBlock>

<ConsoleOutput title="Console output">
Paid 49 by card
Paid 49 by PayPal
</ConsoleOutput>

## When to use

Several variants of the same process and dynamic selection.
